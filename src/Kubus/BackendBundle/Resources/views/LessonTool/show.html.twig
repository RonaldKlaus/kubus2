{% extends 'KubusBackendBundle::layout.html.twig' %}

{% block content -%}
    <h1>LessonTool</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Lessonid</th>
                <td>{{ entity.lessonId }}</td>
            </tr>
            <tr>
                <th>Toolid</th>
                <td>{{ entity.toolId }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Updatedat</th>
                <td>{{ entity.updatedAt|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('lessontool') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('lessontool_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
