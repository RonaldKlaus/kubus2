{% extends 'KubusBackendBundle::layout.html.twig' %}

{% block content -%}
    <h1>LessonTool list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Lessonid</th>
                <th>Toolid</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lessontool_show', { 'id': entity.id }) }}">{{ entity.lessonId }}</a></td>
                <td>{{ entity.toolId }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lessontool_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lessontool_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lessontool_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
