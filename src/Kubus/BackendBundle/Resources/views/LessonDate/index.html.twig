{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LessonDate list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Lessonid</th>
                <th>Locationid</th>
                <th>Beginat</th>
                <th>Endat</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lessondate_show', { 'id': entity.id }) }}">{{ entity.lessonId }}</a></td>
                <td>{{ entity.locationId }}</td>
                <td>{% if entity.beginAt %}{{ entity.beginAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.endAt %}{{ entity.endAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lessondate_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lessondate_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lessondate_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
