{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LessonLocation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Street</th>
                <th>Postcode</th>
                <th>City</th>
                <th>Country</th>
                <th>Room</th>
                <th>Description</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lessonlocation_show', { 'id': entity.id }) }}">{{ entity.street }}</a></td>
                <td>{{ entity.postcode }}</td>
                <td>{{ entity.city }}</td>
                <td>{{ entity.country }}</td>
                <td>{{ entity.room }}</td>
                <td>{{ entity.description }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lessonlocation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lessonlocation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lessonlocation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
