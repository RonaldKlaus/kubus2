{% extends 'KubusBackendBundle::layout.html.twig' %}

{% block content -%}
    <h1>Course list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Number</th>
                <th>Label</th>
                <th>Labelshort</th>
                <th>Description</th>
                <th>Programid</th>
                <th>Audienceid</th>
                <th>Hours</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Subcategoryid</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('course_show', { 'id': entity.id }) }}">{{ entity.number }}</a></td>
                <td>{{ entity.label }}</td>
                <td>{{ entity.labelShort }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.programId }}</td>
                <td>{{ entity.audienceId }}</td>
                <td>{{ entity.hours }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.subCategoryId }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('course_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('course_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('course_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
