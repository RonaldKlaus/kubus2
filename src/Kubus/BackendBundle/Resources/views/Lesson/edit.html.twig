{% extends 'KubusBackendBundle::layout.html.twig' %}

{% block content -%}
    <h1>Lesson edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('lesson') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
