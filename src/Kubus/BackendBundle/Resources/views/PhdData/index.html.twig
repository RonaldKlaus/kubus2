{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PhdData list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Personid</th>
                <th>Programid</th>
                <th>Programacceptanceat</th>
                <th>Phdacceptanceat</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('phddata_show', { 'id': entity.id }) }}">{{ entity.personId }}</a></td>
                <td>{{ entity.programId }}</td>
                <td>{% if entity.programAcceptanceAt %}{{ entity.programAcceptanceAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.phdAcceptanceAt %}{{ entity.phdAcceptanceAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('phddata_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('phddata_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('phddata_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
